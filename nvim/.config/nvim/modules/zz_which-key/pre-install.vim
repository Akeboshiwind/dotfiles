" pre-install.vim


" >> Initial setup

" Setup the map which contains the documentation
let g:which_key_map = {}

" Use a floating window if available
let g:which_key_use_floating_win = 1

" Have the floating window be in a common place
" i.e. not relative the the current window
let g:which_key_floating_relative_win = 0

" Don't have a gap to the left for the sign column
let g:which_key_disable_default_offset = 1

" Have the menu left align
let g:which_key_centered = 0

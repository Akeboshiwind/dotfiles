{:plan [;; >> Bootstrap (provides :pkg/brew, :pkg/mise, :pkg/mas, :pkg/bbin)
        {:pkg/script
         {:homebrew
          {:src "command -v brew >/dev/null 2>&1 || /bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\""
           :dep/provides #{:pkg/brew}}}
         :pkg/brew
         {:mise {:dep/provides #{:pkg/mise}}
          :mas {:dep/provides #{:pkg/mas}}
          "babashka/brew/bbin" {:dep/provides #{:pkg/bbin}}}}

        ;; >> OSX
        :macos

        ;; >> Terminal setup
        :alacritty
        :tmux
        :fish
        :bash

        ;; >> Editor
        :nvim
        :vim
        :intellij

        ;; >> Tools
        :git
        :ssh
        :gpg
        :fzf
        :zoxide
        :bin

        ;; >> Languages
        :clojure
        :terraform
        :python

        ;; >> Deployment
        :aws

        ;; >> AI
        :llm

        ;; >> Work
        {:pkg/brew {:slack {}}
         :pkg/mas {"Windows App" {:id 1295203466}}}

        {:pkg/brew {"postgresql@18" {}}}

        ;; >> Fun
        {:pkg/brew {:godot {}
                    :telegram {}
                    :musescore {}}}]}
